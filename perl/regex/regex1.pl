#!/usr/bin/perl
#
#


$firstname = "Full";
$fullname = "Full Name";
@array1 = ("Full Name", "Full Name");


        if ($fullname =~ /Full/i) { # the i tells perl to ignore case
            print "$firstname\n";
        }
            
            
